import { Model, BaseModel, Uuid } from '@discord-factory/storage-next'

@Model('$tableName')
export default class $modelName extends BaseModel {
  public id: string

  public static beforeInsert (model: $modelName) {
    model.id = Uuid.generateV4()
  }
}